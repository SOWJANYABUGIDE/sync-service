server:
  port: 8080

spring:
  application:
    name: event-sync-service
  jackson:
    default-property-inclusion: non_null

event-sync:
  local-zone: "America/New_York"
  match:
    time-window-minutes: 150
    min-score: 0.55

logging:
  level:
    com.firma.eventsync: INFO
